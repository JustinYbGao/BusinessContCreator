{
  "name": "@social-agent/visual-engine",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run src/visual.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@social-agent/contracts": "workspace:*",
    "playwright": "1.55.0",
    "sharp": "^0.35.3"
  }
}
